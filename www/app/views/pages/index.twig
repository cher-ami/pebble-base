
{% extends "layouts/mainLayout.twig" %}

{% block content %}

	<!-- APP DOM -->
	<div class="AppView">
		<h1>{{ config.dictionary.currentPage.title }}</h1>
		<p>{{ config.dictionary.currentPage.content }}</p>
	</div>

	<!-- APP SCRIPTS -->
	<script>
		/*
		requirejs(['src/moduleName/Main'], function (MainModule)
		{
			var app = new MainModule.Main({
				root		: $('.AppView'),
				host		: "{{ config.server.host }}",
				base		: "{{ config.server.base }}"
			});
		});
		*/
	</script>
{% endblock %}